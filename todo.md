### Task 1: User Management Form and Table

Create a **User Management** screen with a **Create User** button above the existing Tabulator table.

- Clicking **Create User** should open a modal popup.
- Add **User Name, ID, and Password** fields in the modal.
- Reuse the **existing Tabulator table component** to display user data.
- Add basic form validation and ensure existing functionality is not affected.
- Test the implementation with multiple scenarios, including valid input, empty fields, modal close/cancel, multiple users, and existing table functionality.
- Provide the **test results with PASS/FAIL status** for each scenario.
- If any test fails, fix **only the failed scenario** and retest it.

Read `TODO.md` and implement **Task 2**.

- Add **Edit** and **Delete** actions in the User Management table's **Action** column.
- Verify the actions are correctly positioned, aligned, and working.
- Test multiple users and ensure existing table functionality is not affected.
- If any test fails, fix **only the failed scenario** and retest it.
- Provide the test results with PASS/FAIL status.
- After all tests pass, create a feature branch, commit and push the changes, create a PR, review it, and merge it into the required branch.
- Report the implementation, test results, PR, and merge status.
